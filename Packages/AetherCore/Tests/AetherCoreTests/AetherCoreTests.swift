import XCTest
@testable import AetherFoundation
@testable import AetherServices
@testable import AetherDesign
@testable import AetherUI

final class AetherCoreTests: XCTestCase {
    func testPackageScaffold() {
        // 占位测试，验证 Package 脚手架可编译
        XCTAssertTrue(true)
    }
}
